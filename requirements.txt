# Direct dependencies only, pinned to the versions the pipeline ran in
# production. Transitive dependencies resolve from these.
# Install:  python3 -m venv venv && ./venv/bin/pip install -r requirements.txt
# Then:     ./venv/bin/playwright install chromium
anthropic==1.2.0
Flask==3.1.3
waitress==3.0.2
playwright==1.62.0
reportlab==5.0.1
pytest==9.1.1
