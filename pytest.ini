; Guard rail: the repo root holds test_smtp.py and friends — CONNECTION
; SCRIPTS kept as reference patterns (see CLAUDE.md), not tests. They execute
; at import; test_smtp.py sends a real email. Restricting collection to
; tests/ makes a bare `pytest` safe: it can never import them.
[pytest]
testpaths = tests
